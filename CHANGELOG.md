# 更新日志

每次发布的新增功能与修复，最新的在最上面。版本号与脚本 `-v` 输出一致。

## v1.3.0 · 2026-09-18

### 新增

- 物理机（独立服务器）读取硬盘 SMART：健康状态、通电时间、温度、寿命已用、累计写入，出现坏道重映射时单独提示
- 物理机列出内存插槽数、最大容量、ECC 与每条内存的容量、代数、频率、厂商型号
- 系统能读到时显示 CPU、硬盘、主板温度
- Geekbench 6 跑分：`-g`，全部检测默认包含；结果按 Geekbench 官方规则公开上传，报告附结果页链接；内存或磁盘空间不足、下载太慢时跳过并说明原因
- NAT 类型细分为公网直连、防火墙、全锥形（NAT1）、受限锥形（NAT2）、端口限制锥形（NAT3）、对称（NAT4），附对联机与 P2P 的影响说明
- 有 IPv6 时加测三网 IPv6 延迟与 IPv6 回程线路，回程详情分 IPv4 / IPv6 两段；体检总览附 IPv6 平均延迟
- 分省测速：`-p`，全部检测默认包含；北京、天津、上海、江苏、浙江、福建、湖北、湖南、四川的三网节点，连不上的标为不可达
- 物理机缺少 smartmontools、dmidecode 时与 sysbench、fio 一起询问安装
- 结果页：每次检测结束给出一个 10 位编号的地址 `https://sh.cd/results/…`，浏览器里按总览、硬件、IP、网络、回程分页查看，顶部列出硬件、IP、三网回程与带宽要点
- 结果页一键复制链接、复制 Markdown、下载 .md，也可以复制单独一段；终端里用 `curl -s` 打开同一地址直接看报告
- 结果页里的 IP 与终端报告一样只显示前两段，不进搜索引擎，保存 365 天；加 `-P` 不生成结果页，`-j` 输出 JSON 时也不生成
- 国际延迟从 12 个点扩到 31 个，按大洲分行显示：亚洲 11（新增吉隆坡、曼谷、雅加达、马尼拉、胡志明市、孟买）、中东 3（迪拜、利雅得、特拉维夫）、欧洲 6（新增马德里、华沙）、非洲 3（约翰内斯堡、开罗、卡萨布兰卡）、北美 4（新增达拉斯、多伦多）、南美 2（圣保罗、圣地亚哥）、大洋洲 2（新增奥克兰）
- 国际延迟节点连不上时自动换同城备用节点
- 国际延迟再加 5 个点，共 36 个：欧洲新增赫尔辛基、莫斯科、伊斯坦布尔，北美新增圣何塞、西雅图
- 全省回程：菜单第 7 项或 `-R`（全部检测默认包含），31 省 × 电信 / 联通 / 移动共 93 条线路各自识别类型，每格显示线路、延迟中位数与丢包，原来的三城回程作为快速版保留在菜单第 6 项
- 大包回程：同一批目标再用 1400 字节的包测一遍，和普通包走不同线路就说明大包绕路或被限速
- 有 IPv6 时，IPv6 回程也按 31 省测；深度模式下每条线路都能看逐跳位置、延迟与 ASN
- 教育网回程：随全省回程一起测，每省一所高校官网，IPv4 走 CERNET、IPv6 走 CERNET2，给出进教育网前最后经过的骨干网、延迟与丢包，境外机器上还会认出 HKIX、Lumen、Tata、PCCW 等国际转接方
- 菜单第 8 项「带宽测速」：只跑测速，含就近、国内三网、国际六地与分省节点，不用等延迟和回程
- 市级延迟：`-c`（全部检测默认包含），223 个市级节点按省分组列出各市的延迟中位数与丢包，比只看省级节点更细

### 修复

- 体检总览的延迟一行过长时按显示宽度换行，不再超出报告宽度
- macOS 自带的 bash 在部分终端里，开头的 CLEAN IP 字符画显示成乱码、没有颜色；硬盘 ATTO 与测速的进度提示末尾同样出现乱码
- 分省测速表里该省没有某运营商节点时显示「-」，不再留空
- 三网延迟与国际延迟扣除域名解析时间，只算 TCP 握手：不缓存 DNS 的机器上解析一次要零点几秒到两秒多，此前会把延迟算高，解析超过 1 秒时还会被误当成重传

## v1.2.0 · 2026-09-17

### 新增

- sh.cd 成为独立服务，脚本、报告与官网统一在 https://sh.cd
- 全新官网：Ioskeley Mono 字体，示例报告分总览 / 硬件 / IP / 网络四个标签，检测内容清单与可复制的常用命令
- 脚本开头显示 CLEAN IP 字符画
- 官网页脚加版权信息
- 全部检测：菜单第 2 项或 `-A -d`，在一键全检的基础上加测硬盘 ATTO 块大小表、回程每一跳的延迟，并输出回程路由详情
- 报告里的 IP 只显示前两段（如 `203.0.*.*`），同网段的网段信息、NAT 出口、反查主机名与回程跳点一并打码，截图可以直接分享；`-j` 导出的 JSON 保留完整数据
- 体检总览底部附上入口命令 `bash <(curl -Ls https://sh.cd)`
- 体检总览的 IP 评分改为综合评分并列纯净度，与 cleanip.io 网页上的分数一致
- 邮箱握手区分「握手成功 / 被拒收 / 连不上」三种结果，被拒收多为对方按 IP 信誉拒绝
- IPv6 出口检测遇到本身不支持 IPv6 的网站（TikTok、Prime Video、Reddit）标为「不支持 IPv6」
- 官网新增更新日志页面
- 官网里的 CleanIP 改用官方 logo

### 修复

- 一键全检中途不再停顿等待确认；缺少 sysbench / fio 时只在开始前询问一次，15 秒不回答默认安装
- 测速节点连不上、不传数据或单向限速时，自动换同组下一个节点；就近节点取最近的 3 个
- 东京测速与延迟节点增加备用
- 修复 macOS 上运行时长、内存容量与 Apple 芯片指令集显示错误
- 修复非 root 用户与 macOS 上检测虚拟化类型时的报错
- 移除备用入口 cleanip.io/ipcheck，只保留 https://sh.cd
- 三网延迟：TCP 握手遇到重传（多出约 1 秒）时扣除重传等待、还原真实延迟并计入丢包，不再显示一千多毫秒的延迟
- 测速：某个方向不到另一方向十分之一时标灰并注明节点限速；体检总览的带宽在就近节点单向受限时改取境外节点
- 邮箱握手等待时间放宽到 10 秒并自动重试一次，修复并发时个别邮箱误报失败
- 修复 macOS 上检测邮箱时终端出现多行 `Killed: 9`
- TikTok 被当地网络封锁时显示为不可用，不再显示检测失败；YouTube Premium 不可用时不再显示错误的地区 US
- CPU 缓存按每个核心显示（此前显示的是所有核心的合计）
- 硬盘已用比例与 `df` 命令显示一致
- 「超开迹象」改为中性的「内存气球」说明，去掉在虚拟机里无法判断的 KSM 项
- 未安装 sysbench 时不再显示不准确的内存读写带宽
- 反查主机名为空时不再重复显示 IP；BGP 上游名称去掉注册句柄与引号
- 体检总览的 IP 类型显示中文；「邮局」统一为「邮箱」
## v1.0.0 · 2026-09-16

### 新增

- 首个版本：IP 纯净度与风险评分、多源检测、黑名单
- Netflix、Disney+、YouTube Premium、TikTok、Prime Video、Reddit、ChatGPT、Claude、Gemini 解锁检测
- 25 端口出站与 12 家邮箱握手
- DNS 出口检测
- 浏览器打开 sh.cd 显示说明页
- 硬件与性能：系统与虚拟化类型、CPU 型号与指令集、sysbench CPU / 内存跑分、fio 硬盘 4K 随机与顺序读写，深度模式加测 ATTO 块大小表
- 网络质量：NAT 类型与 TCP 策略、BGP 上游与对等、全国 31 省三网延迟、北京 / 上海 / 广州三网回程线路识别、国内外带宽测速、12 个国际节点延迟
- 回程路由详情：逐跳显示位置、ASN 与延迟
- 菜单：一键全检或单独检测硬件 / IP / 网络，最后给出一屏体检总览
- 支持 macOS 与 ARM 机器
